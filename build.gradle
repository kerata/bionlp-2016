group 'Boun'
version '1.0-SNAPSHOT'

apply plugin: 'java'

sourceCompatibility = 1.5

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.11'
    compile files('libs/stanford-postagger.jar', 'libs/slf4j-api.jar', 'libs/slf4j-simple.jar')
}
